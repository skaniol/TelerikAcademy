// 04. Declare an integer variable and assign it with the value 254 in hex.

class E04_AssignHexToInt
{
    static void Main()
    {
        int intVar = 0xFE;
        System.Console.WriteLine("intVar == 254 ? -> {0}", intVar == 254);
    }
}